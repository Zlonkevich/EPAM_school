package main;

public class Sign {

    public static int sign(int a) {
        if (a > 0)
            return 1;
        else if (a < 0)
            return -1;
        else
            return 0;
    }
}
